# ================================================================
# SECRETS AND CREDENTIALS ONLY (Tier 3 Configuration)
# ================================================================
# This file contains ONLY API keys, secrets, and credentials.
# Feature flags and business logic configuration are now in:
# src/config/settings.py (version-controlled with sensible defaults)
#
# For local development, you can override ANY setting from settings.py
# by adding the environment variable here (e.g., SIGNAL_GENERATION_LOCAL_SIGNAL_GENERATION_ENABLED=false)

# LLM Provider Configuration
# Provider options: openrouter, openai_direct, anthropic_direct
LLM_PROVIDER="openrouter"

# OpenRouter Configuration (Recommended - access to many models)
OPENROUTER_API_KEY="YOUR-API-KEY-HERE"
LLM_DEFAULT_MODEL="google/gemini-2.5-flash"  # Available via OpenRouter: gpt-4o-mini, claude-3-sonnet, etc.

# Direct OpenAI Configuration (Example Override)
# To use direct OpenAI instead of OpenRouter:
# LLM_PROVIDER="openai_direct"
# LLM_OPENAI_API_KEY="your-openai-key-here"
# LLM_OPENAI_DEFAULT_MODEL="gpt-4o-mini"

# Direct Anthropic Configuration (Example Override)
# To use direct Anthropic instead of OpenRouter:
# LLM_PROVIDER="anthropic_direct"
# LLM_ANTHROPIC_API_KEY="your-anthropic-key-here"
# LLM_ANTHROPIC_DEFAULT_MODEL="claude-3-haiku-20240307"

# Data Provider API Keys
# At least one news provider is required for sentiment analysis
DATA_ALPHA_VANTAGE_API_KEY="YOUR-API-KEY-HERE"
DATA_FINNHUB_API_KEY="YOUR-API-KEY-HERE"
DATA_MARKETAUX_API_KEY="YOUR-API-KEY-HERE"
